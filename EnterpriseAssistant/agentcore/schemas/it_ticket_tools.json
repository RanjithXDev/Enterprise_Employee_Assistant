[
  {
    "name": "get_ticket_details",
    "description": "Retrieve IT ticket details by ticket ID for an authorized employee or IT administrator.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "ticket_id": {
          "type": "string",
          "description": "The IT ticket ID, for example INC-1001."
        },
        "requester_id": {
          "type": "string",
          "description": "Authenticated requester ID supplied by the application."
        },
        "user_role": {
          "type": "string",
          "enum": [
            "Employee",
            "ITAdmin"
          ],
          "description": "Authenticated user role supplied by the application."
        }
      },
      "required": [
        "ticket_id",
        "requester_id",
        "user_role"
      ]
    }
  }
]